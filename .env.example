# Armbian / CasaOS user and group IDs (run `id` to get these)
PUID=1000
PGID=1000

# Timezone (e.g. Asia/Jakarta)
TZ=Asia/Jakarta

# ---- From-scratch app (see docs/plan-from-scratch.md) ----
# ROOT=/data
# DB_PATH=/data/db/app.sqlite
# SESSION_SECRET=<long-random-string>
# PORT=8080
# DEFAULT_ADMIN_USER=admin
# DEFAULT_ADMIN_PASSWORD=admin
# APP_NAME=Studio Photos
# Optional optimization (docs/optimization-recommendations.md): READ_HEADER_TIMEOUT, READ_TIMEOUT, WRITE_TIMEOUT, MAX_UPLOAD_BYTES, MAX_REQUEST_BYTES, THUMB_*, ZIP_MAX_*, SQLITE_BUSY_TIMEOUT, STATIC_CACHE_MAX_AGE
