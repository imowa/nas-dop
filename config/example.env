# Copy to repo root as .env. See docs/plan-from-scratch.md §2b.
# Source before run or use env_file in Docker. Set SESSION_SECRET in production.

# Core
ROOT=/data
DB_PATH=/data/db/app.sqlite
SESSION_SECRET=change-me-to-a-long-random-string
PORT=8080
DEFAULT_ADMIN_USER=admin
DEFAULT_ADMIN_PASSWORD=admin
APP_NAME=Studio Photos
PUID=1000
PGID=1000
TZ=Asia/Jakarta

# Optimization (docs/optimization-recommendations.md). Omit to use defaults.
# READ_HEADER_TIMEOUT=10s
# READ_TIMEOUT=30s
# WRITE_TIMEOUT=60s
# MAX_UPLOAD_BYTES=104857600
# MAX_REQUEST_BYTES=524288000
# THUMB_MAX_SIZE_SHARE=200
# THUMB_MAX_SIZE_ADMIN=320
# THUMB_CONCURRENCY=4
# ZIP_MAX_FILES=500
# ZIP_MAX_BYTES=2147483648
# SQLITE_BUSY_TIMEOUT=5s
# STATIC_CACHE_MAX_AGE=86400
